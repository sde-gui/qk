#!/bin/sh

for f in pot dist; do if test ! -e $f; then ln -s maintain $f; fi; done

case `basename $0` in
    pot)
        args="--gettext-package=moo -p"
        ;;
    dist)
        args="--gettext-package=moo -d"
        ;;
    *)
        args="--gettext-package=moo -m"
        ;;
esac

exec intltool-update $args $*
